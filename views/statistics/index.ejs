<!DOCTYPE html>
<html>
  <%- include('../head'); -%>
  <body>
    <%- include('../navbar', {active: 'statistics'}); -%>
    <div class="container-fluid">
      <div class="row">
        <div class="col-auto p-3 border-right">
          <nav class="nav nav-pills flex-column">
            <%_ for (const s of statistics) { -%>

              <a href="/statistics/<%= s.id -%>" class="nav-item nav-link"><%= s.county -%></a>
            <%_ } -%>
          </nav>
          <%_ if (admin) { -%>
          <form id="create" class="form my-1">
            <input type="text" name="_id" placeholder="CS###" size="1" class="form-control">
          </form>
        <%_ } -%>
        </div>
        <div class="col">
          <main class="p-3">
            <%_ if (admin) { -%>
              <%- include('./edit', {statistics: statistics, statisticID: statisticID}); -%>
            <%_ } else { -%>
              <%- include('./browse', {statistics: statistics}); -%>
            <%_ } -%>
              </tbody>
            </table>
          </main>
        </div>
      </div>
    </div>
    <%_ if (admin) { -%>
      <%- include('./scripts', {statistics: statistics}); -%>
    <%_ } -%>
  </body>
</html>
