<div class="container-fluid">
  <div class="col">
    <div class="col-auto p-3 border-right">
       <h1>Q&A</h1>

       <form>
     <input type="text" id="search" placeholder="Question">

  <button>Search</button>
   </form>
   <h2>Result</h2>
   <div id="searchresult" rows="5" cols="85">

   </div>


      <nav class="nav nav-pills flex-column">
        <form>
         <input type="text" name="question" placeholder="Question" class="form-control form-control-lg font-weight-bold mb-1">
         <button id="insert" class="btn btn-primary">Submit</button>
       </form>
       <h2>Answerd Question</h2>
        <%_ for (const qa of qas) { -%>
          <h2><%= qa.question -%></h2>
          <p><%= qa.answer -%></p>
          <%_ } -%>
        </nav>
      </div>
    </div>
    <script src="https://code.jquery.com/jquery-3.4.1.min.js" integrity="sha256-CSXorXvZcTkaix6Yvo6HppcZGetbYMGWSFlBw8HfCJo=" crossorigin="anonymous"></script>
    <script>
      // Intercept form submits
      $('form').on('submit', function(event) {
        event.preventDefault();
        // Construct a request object
        const request = {
          data: $('search').val()
        };
        // Send the request and display the response on the page
        $.ajax(request)
          .done(response => $('searchresult').val(JSON.stringify(response)))
          .fail(error => $('searchresult').val(JSON.stringify(error)));
      });
      </script>
  </div>
