<!DOCTYPE html>
<html>
<%- include('../head'); -%>
<body>
  <%- include('../navbar', {active: 'qas'}); -%>
  <div class="container-fluid">
    <div class="col">
      <div class="col-auto p-3 border-right">
        <nav class="nav nav-pills flex-column">
          <%_ for (const id of qaId) { -%>
            <%_ const status = (id === qa.id) ? 'active bg-dark' : 'inactive'; -%>
            <a href="/qas/<%= qa.question -%>" class="<%= status -%>"><%= qa.question -%></a>
          <%_ } -%>
        </nav>
      </div>
      <main class="p-3">
        <h2><%= qa.question -%></h2>
        <p><%= qa.answer -%></p>
      </main>
      </div>
    </div>
  </div>
</body>
</html>
