<!DOCTYPE html>
<html>
  <%- include('../head'); -%>
  <body>
    <%- include('../navbar', {active: 'counties'}); -%>
    <div class="container-fluid">
      <div class="row">
        <div class="col-auto p-3 border-right">
          <nav class="nav nav-pills flex-column">
            <%_ for (const id of countyID) { -%>
              <%_ const status = (id === county.id) ? 'active bg-dark' : 'inactive'; -%>
              <a href="/counties/<%= id -%>" class="nav-item nav-link <%= status -%>"><%= id -%></a>
            <%_ } -%>
          </nav>
        </div>
        <div class="col">
          <main class="p-3">
            <h2><%= county.name -%></h2>
            <p><%= county.day -%></p>
            <p><%= county.statistics.confirmed -%></p>
          </main>
        </div>
      </div>
    </div>
  </body>
</html>
